const PaymentGateway = () => (<><div className="page-header"><h3 className="page-title">Payment Gateway</h3></div><div className="card"><div className="card-body"><div className="row">{[{ name: 'PayPal', status: true }, { name: 'Stripe', status: true }, { name: 'Razorpay', status: false }, { name: 'Bank Transfer', status: true }].map((g, i) => <div className="col-md-3 mb-3" key={i}><div className="card"><div className="card-body text-center"><h5>{g.name}</h5><span className={`badge bg-${g.status ? 'success' : 'secondary'}`}>{g.status ? 'Active' : 'Inactive'}</span><br /><button className="btn btn-sm btn-outline-primary mt-2">Configure</button></div></div></div>)}</div></div></div></>)
export default PaymentGateway
